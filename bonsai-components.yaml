# Build phase configuration for Bonsai packages

# Libraries: External packages that can be built in parallel
libraries:
  # - "@bonsai/remeda"
  - "@bonsai/rxjs"
  # - "@bonsai/zod"

# Packages: Our in-house packages with possible interdependencies
packages:
  - "@bonsai/types"
  - "@bonsai/event"
  # Add other packages here as needed
